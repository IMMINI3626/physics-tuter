# PhysiClinic — 설계 기준 및 미결 사항

이 문서는 코드가 지금도 참조하는 살아있는 기준(소단원 분류)과, 아직 결론이 나지 않은
사항을 담는다. 기능별 상세 스펙과 구현 이력은 [CHANGELOG.md](CHANGELOG.md)에,
진행 중인 학습자 모델(BKT) 설계는 [docs/오개념측정_BKT_설계.md](docs/오개념측정_BKT_설계.md)에 있다.

---

## 소단원 분류 (UNIT_MAP)

레벨 시스템은 소단원 단위로 관리하고, 마이페이지에서는 대단원으로 묶어 시각화한다.
소단원-대단원 매핑은 Firestore가 아니라 프론트 상수(`public/js/app.js`의 UNIT_MAP)로 둔다.

```js
const UNIT_MAP = {
  '힘과 운동': {
    chapterId: '1',
    subUnits: ['물체의 운동', '뉴턴 운동 법칙', '운동량과 충격량']
  },
  '에너지': {
    chapterId: '2',
    subUnits: ['역학적 에너지 보존', '열역학 법칙', '특수 상대성 이론']
  },
  '전기와 자기': {
    chapterId: '3',
    subUnits: ['원자 모형과 전기력', '에너지 띠와 반도체', '전류의 자기 작용', '전자기 유도']
  },
  '파동': {
    chapterId: '4',
    subUnits: ['파동의 진동과 굴절', '파동의 간섭', '빛의 이중성', '물질의 이중성']
  },
};

// 소단원 → 대단원 찾기
function getChapter(unitName) {
  for (const [chapter, data] of Object.entries(UNIT_MAP)) {
    if (data.subUnits.includes(unitName)) return chapter;
  }
  return null;
}
```

주의: 이 표는 `public/js/app.js`의 실제 값을 그대로 옮긴 것이다. 소단원을 추가·변경하면
이 문서와 함께, `functions/index.js`의 extractKeywords 프롬프트에 있는 소단원 리스트도
동일하게 유지해야 한다. 그래야 Gemini가 반환하는 소단원명이 UNIT_MAP 키와 어긋나지 않는다.

---

## 미결 사항

- 교육과정 분류 정정 필요. 현재 UNIT_MAP은 '특수 상대성 이론'을 '에너지'에, '원자 모형과
  전기력'을 '전기와 자기'에 넣고 있는데, 실제 물리학Ⅰ 교과서 목차와 다르다. 또한 이 표가
  `app.js` 상수와 `functions/index.js` 프롬프트 두 곳에 중복되어 동기화가 수동이다.
  Phase 3에서 UNIT_MAP을 Firestore로 옮길 때 함께 정리할 예정.

- Level 3 풀이 과정 채점 정확도. 실사용으로 확인하며, 부정확하면 AI가 풀이 과정은 채점하지
  않고 피드백만 제공하는 방식으로 교체할 수 있게 분리해 두었다.

- 레벨 시스템 도입 이전(2026-04~05) 세션은 unitProgress 문서가 없어 마이페이지에 "시작 전"으로
  뜬다. 과거 세션으로 소급 생성하는 백필을 검토했으나, 그 시절엔 레벨 개념이 없어 정확한
  레벨을 복원할 근거가 없어 보류했다. 필요하면 세션 개수·최고점·마지막 학습일만 채우고
  레벨은 1로 시작하는 방식으로 재검토.

- 소단원 상세의 점수 추이 그래프와 이력 목록이 "다시 풀기" 재도전을 다르게 취급한다.
  그래프는 재도전까지 개별 점으로 찍고, 이력 목록은 같은 문제 재도전을 그룹으로 묶는다.
  각자 목적(추이 확인 / 정리해서 보기)에 맞춘 의도적 차이라 버그는 아니다. 통일이 필요하면
  그래프도 문제 단위(그룹당 최신 점수 하나)로 그리도록 변경. 상세는 CHANGELOG 3-4절 참고.

## 해결된 이슈

- 마이페이지 "교정된 오개념" 통계가 실데이터 기반이 아니던 문제 (2026-07). 세션에 "다룬
  오개념 id"만 있고 문항 단위 정오답과 연결이 없어 교정 여부를 정확히 집계할 수 없었다.
  당시엔 통계 카드를 제거하고, 대신 단원별 평균 점수 기반 취약 단원 표시로 대체했다.
  이후 Phase 2에서 "집중하면 좋을 개념"(개념 영역별 반복 집계)으로 개편했고, Phase 3의
  BKT 도입으로 문항별 정오답을 추적하게 되면 이해도 확률 기반으로 교정 통계를 되살릴 수 있다.
